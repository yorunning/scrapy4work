add `db_info.json` to `./work/json`

```json
{
  "host": "host",
  "user": "yorun",
  "password": "123",
  "database": "work"
}
```

add `proxy.json` to `./work/json`

```json
{
  "http_proxy": "ip:port",
  "splash_url": "ip:8050"
}
```