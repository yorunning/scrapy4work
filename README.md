add `db_info.json` to `./work/resource`

```json
{
  "host": "host",
  "user": "username",
  "password": "password",
  "database": "database"
}
```

add `proxy.json` to `./work/resource`

```json
{
  "proxy_url": "ip:port",
  "splash_url": "ip:8050"
}
```